// import { PaletteMode } from "@mui/material"

export type ThemeShape = {
    title: string
    description?: string
    primaryColor: string
    secondaryColor: string
    font: string
    // mode: PaletteMode
}